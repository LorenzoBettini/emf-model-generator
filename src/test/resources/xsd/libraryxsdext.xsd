<?xml version="1.0" encoding="UTF-8"?>
<xsd:schema targetNamespace="http://www.example.eclipse.org/Library"
    xmlns:ecore="http://www.eclipse.org/emf/2002/Ecore"
    xmlns:lib="http://www.example.eclipse.org/Library"
    xmlns:xsd="http://www.w3.org/2001/XMLSchema">

  <!--
    A global element makes the schema represent an XML document rooted at
    "library". EMF's XSD importer typically represents global elements through
    DocumentRoot and ExtendedMetaData in the generated Ecore model.
  -->
  <xsd:element name="library" type="lib:Library"/>

  <xsd:complexType name="Book">
    <xsd:sequence>
      <xsd:element name="title" type="xsd:string"/>
      <xsd:element name="pages" type="xsd:int"/>
      <xsd:element name="category" type="lib:BookCategory"/>
      <xsd:element name="author" type="xsd:anyURI"
          ecore:reference="lib:Writer" ecore:opposite="books"/>
    </xsd:sequence>
  </xsd:complexType>

  <xsd:complexType name="Writer">
    <xsd:sequence>
      <xsd:element name="name" type="xsd:string"/>
      <xsd:element maxOccurs="unbounded" minOccurs="0" name="books"
          type="xsd:anyURI" ecore:reference="lib:Book" ecore:opposite="author"/>
    </xsd:sequence>
  </xsd:complexType>

  <xsd:complexType name="Library">
    <xsd:sequence>
      <xsd:element name="name" type="xsd:string"/>

      <!--
        The repeated heterogeneous choice is deliberate. EMF's XSD importer
        typically represents this kind of content model with a FeatureMap group
        plus derived member features for "writer" and "book". This makes the
        generated Ecore useful for testing XSD-originated feature-map handling.
      -->
      <xsd:choice minOccurs="0" maxOccurs="unbounded">
        <xsd:element name="writer" type="lib:Writer"/>
        <xsd:element name="book" type="lib:Book"/>
      </xsd:choice>
    </xsd:sequence>
  </xsd:complexType>

  <xsd:simpleType name="BookCategory">
    <xsd:restriction base="xsd:NCName">
      <xsd:enumeration value="Mystery"/>
      <xsd:enumeration value="ScienceFiction"/>
      <xsd:enumeration value="Biography"/>
    </xsd:restriction>
  </xsd:simpleType>

</xsd:schema>
